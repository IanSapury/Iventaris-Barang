[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm --prefix backend install --omit=dev"]

[phases.build]
cmds = ["echo 'Build phase complete'"]

[start]
cmd = "node backend/server.js"

[variables]
NODE_ENV = "production"
